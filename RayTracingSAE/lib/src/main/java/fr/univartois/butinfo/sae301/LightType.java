package fr.univartois.butinfo.sae301;

public enum LightType {
    DIRECTIONAL, // Represents a directional light source that emits light in a specific direction.
    
    POINT     // Represents a point light source that emits light from a specific point in space.
}
